# 头像内部资源

- `style-v1.png`：由已批准提交 `b6fe15a8c11270d3c1568f7f40af08484ce71fd3` 的原创 `prototypes/map-home/characters.mjs` 两名角色 idle 图绘制为并排风格参考，392×200；仅在显式 FLUX 请求中作为画风参考，不作为用户成品。
- `test-candidate.svg`：同源原创 idle 单人素材，仅受控自动测试 provider 使用。不是 AI 输出、不基于真实自拍；production 不能启用 fixture。

资源保留宽圆大头、像素阶梯轮廓、清楚五官和极少肩部，去掉状态道具。真实用户最终图必须由生成服务返回、规范化并经本人明确确认，存在私密存储，不能将这两个文件分配给真实用户。
